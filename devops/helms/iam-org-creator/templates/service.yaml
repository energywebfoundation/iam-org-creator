apiVersion: v1
kind: Service
metadata:
  name: {{ include "iam-org-creator.fullname" . }}
  labels:
    {{- include "iam-org-creator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "iam-org-creator.selectorLabels" . | nindent 4 }}
