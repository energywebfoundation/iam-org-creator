apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "iam-org-creator.fullname" . }}-test-connection"
  labels:
    {{- include "iam-org-creator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "iam-org-creator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
